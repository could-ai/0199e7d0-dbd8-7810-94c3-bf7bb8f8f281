import 'package:flutter/material.dart';

class AuthProvider with ChangeNotifier {
  // This class will hold authentication state and logic.
  // For now, it's a placeholder to resolve compilation errors.
}
